import { styled } from '@mui/system';

  export const AnimationWrapper = styled('div')({
    display: "flex",
    justifyContent: "center", 
    alignItems: "center", 
    height: "100vh",
    maxWidth: "350px"
  });

  export const NoResaulsWrapper = styled('div')({
    display: "flex",
    justifyContent: "center", 
    alignItems: "center", 
    flexDirection: "column"
  });